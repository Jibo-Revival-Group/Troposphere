# /etc/inittab
#
# Copyright (C) 2001 Erik Andersen <andersen@codepoet.org>
#
# Note: BusyBox init doesn't support runlevels.  The runlevels field is
# completely ignored by BusyBox init. If you want runlevels, use
# sysvinit.
#
# Format for each entry: <id>:<runlevels>:<action>:<process>
#
# id        == tty to run on, or empty for /dev/console
# runlevels == ignored
# action    == one of sysinit, respawn, askfirst, wait, and once
# process   == program to run

# Startup the system
::sysinit:/bin/mount -t proc proc /proc
::sysinit:/bin/mkdir -p /dev/pts
::sysinit:/bin/mkdir -p /dev/shm
::sysinit:/bin/mount -a
# Feed the watchdog as early as possible and 
# certainly before resizing the file systems
::sysinit:/sbin/watchdog -t 50 /dev/watchdog
# first boot resize was moved here to reduce the number of
# rootfs reboots
::sysinit:/var/etc/first_boot_resize
# move ldconfig call to before remount for
# readonly rootfs
::sysinit:/sbin/ldconfig -v /etc/ld.so.conf.d
# relocated logic from deleted init script
::sysinit:/usr/bin/gdk-pixbuf-query-loaders --update-cache
::sysinit:/bin/mount -o remount,ro /
::sysinit:/bin/hostname -F /etc/hostname
# now run any rc scripts
::sysinit:/etc/init.d/rcS

# Put a getty on the serial port
ttyS0::respawn:/sbin/getty -L -l /usr/bin/jibo-login-wrapper ttyS0 115200 vt100 # GENERIC_SERIAL

# Stuff to do for the 3-finger salute
::ctrlaltdel:/sbin/reboot

# Stuff to do before rebooting
::shutdown:/etc/init.d/rcK
::shutdown:/sbin/swapoff -a
::shutdown:/bin/umount -a -r
